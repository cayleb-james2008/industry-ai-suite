# Lane 2 execution plan

Run `four-lanes-cycle-20260925`; full rung, normal three-round cap for any code piece. The sealed objective is `bar.md` (SHA-256 `b0a4455998b2fb36eec419da8afca8f5bc99c814cab3ab918591929ef7ce1606`); the sealed specification is `specs/spec-lane2-portfolio/SPEC.md` (SHA-256 `ea4b46c768bc36a10cb4f11151bd8734ab44844bcfedae3d7f7bfb1877befb10`). Both exist before this plan.

## Applicability and route

- CLI-Anything-first check: target is the suite's own Python source/CLI, not an external application with an installed backend. `command -v cli-anything` returned no executable. The suite documents its standard-library Python entrypoints; do not install or clone another harness.
- OpenViking recall and the Gauntlet task tool are available. The lead read scoped memory and its top hit. The `gauntlet_spec_author` and `gauntlet_bar_author` tasks both completed. Linux RAM preflight: `RAM SAFE — available 45.1 GiB of 62.4 GiB` before delegation.
- V1 is complete: ten `--help` checks and ten normal app CLIs exited 0; `run_all.py` exited 1 with the documented `INCOMPLETE` state and ten receipts; `suite_core doctor` exited 0 with AI unavailable. Full details and receipt hashes are in `evidence/baseline-assessment.md` (SHA-256 `a3745e644c6f61f3e6e22277e6b1e9161bf308076fd2aaa193d41b888c013d48`). No reproducible app CLI breakage was found, so I1 is skipped.
- V1 selected exactly three: ChainWatch (no admitted source/output), PipelineRelay (fixed one-repository metadata only), BacktestGuard (public chronology split only, no experiment record). Their exact baseline and why the other seven were not selected are in the evidence file.
- Design step is **conditional**: if a local portfolio text change is warranted, the surface is an existing public showcase, so run `gauntlet_design_director` first for a truthful copy-only brief that preserves the existing visual system; then delegate materialization to `gauntlet_frontend_builder`. No layout, branding, or styling changes are in scope. If evidence shows no copy correction is needed, record design-step N/A (no frontend implementation).

## Bounded work map

### V1 — Lead-owned fresh user-run baseline (verification; no implementation)

- **Outcome:** fresh `run_all.py` + `suite_core doctor` receipts, then the documented normal command for each app, with app-README setup/output rules and per-output source/AI/quality assessment.
- **Scope:** read-only app/source/site files. New evidence may be written only under this repository's `session-artifacts/four-lanes-cycle-20260925/evidence/`. Do not alter application code or either repository's tracked content during this stage.
- **Do:** confirm Python/version and documented no-install setup; run the aggregate commands; run the ten normal app entrypoints; inspect each emitted result; match provider, terms, task fit, as-of, response hash, uncertainty and handoff to `LIVE-SOURCE-CONTRACT.md`; check AI/fallback labels against current doctor/receipt evidence; compare live route evidence after the app baseline.
- **Context:** all ten app READMEs plus root README and source/core contracts; historic proof is explicitly limited to the artifacts in `specs/spec-lane2-portfolio/baseline-and-proof.md`.
- **Verify:** exact commands, UTC times, exit codes, output paths and hashes; no demo/fixture path. A full-job label stays WIP/INCOMPLETE/UNVERIFIED without entire current-job evidence.
- **Preserve:** no external effects, no credentials, no AI service startup, no remote writes, no changes to either Git tree.
- **DoD:** all ten outcomes contain setup, normal-path result, output-quality disposition, exact source/authorization claim, AI/fallback label, and evidence pointer; current live portfolio claims are captured and compared or marked unverified.
- **Owner:** lead, because the acceptance bar requires lead-run evidence. This is not a code/build piece.

### I1 — Repair only reproduced journey breakages (skipped)

- **Outcome:** remove a concrete breakage reproduced during V1, if one exists.
- **Scope:** to be narrowed to the exact app/core/test/doc files implicated by the fresh receipt before spawning a worker. If no breakage is reproduced, skip this piece and record “no implementation required.”
- **Do:** reproduce the observed failure; make the smallest truthful fix; add/retain regression coverage.
- **Context:** sealed bar, app README, existing source/core contract, V1 receipt and relevant code only.
- **Verify:** worker tests and thermo review; lead reruns the exact failed path and preservation checks.
- **Preserve:** no test weakening, new source without term review, direct app networking, private-data inference, label promotion, or remote publication.
- **DoD:** observed failure no longer reproduces and the same receipt-quality/source/AI checks still hold; paired blind text + scoring critics pass, or typed bailout records an exact blocker.
- **Disposition:** skipped; all ten normal CLI entrypoints returned 0. No unobserved bug fix will be created.

### R1 — Independent source-term and target-authority check (complete; no code)

- **Outcome:** an independent read-only evidence brief decides whether the Mempool read-only public transaction-history endpoint may be used for the explicit My First Bitcoin public transparency example, under the current Mempool ToS and rate-limit rules.
- **Scope:** no project edits and no API/data request. Read only official Mempool ToS/API documentation and the official My First Bitcoin donation/transparency page; do not read/write any account or contact anyone.
- **Do:** quote what terms actually say, distinguish access terms from a reuse license, record rate-limit caveats, and confirm that the address is owner-published for public treasury transparency—not a Cayleb/company address. If terms do not support one bounded GET or the target authority is ambiguous, recommend BLOCKED and ChainWatch stays no-use.
- **Verify:** source URLs, fresh retrieval UTC, exact relevant text, honest GREEN/UNVERIFIED verdict; return a compact brief and OpenViking evidence. Result: researcher `UNVERIFIED` for current API-doc rendering; independent text critic agreed the no-use path was correct; scoring critic returned fail. Final lead disposition: no Mempool use, because the exact current endpoint/rate-limit terms are not sufficiently verified.
- **Owner:** delegated `gauntlet_researcher`; no source code changes.

### D1 — ChainWatch source admission (blocked before implementation)

- **Outcome:** decide whether the current Mempool terms and endpoint documentation support the owner-published public transparency address.
- **Scope:** no code edits and no further API request.
- **Do:** preserve the researcher and critic findings. The ToS covers associated API services, and the address is owner-published, but the current docs route was an empty cached snapshot and no exact endpoint/use basis was independently confirmed. The lead retracts the tentative one-GET admission.
- **DoD/disposition:** typed `BLOCKED_EXTERNAL_DEPENDENCY` bailout before source-adapter edits; the normal ChainWatch command remains no-request and `UNVERIFIED`. The one earlier test GET is a response observation only.

### D2, D3, D5 — Three weakest eligible deepening pieces

- **Outcome:** each selected app has a distinct, meaningful end-to-end user workflow on a real admissible integration path; missing private job inputs stay explicitly unverified.
- **Scope:** PipelineRelay, BacktestGuard, and OnboardPath only. Each exact file list and budget is frozen in its individual piece contract before spawning. No ChainWatch source/provider/app edits are allowed after D1's no-use bailout. PipelineRelay may change only its own CLI/workflow/tests/docs and the root README row. BacktestGuard may change only its own CLI/workflow/tests/docs. OnboardPath may change only its app workflow/CLI/tests/docs.
- **Do:** D2 makes public repository owner/repo a user-selectable input through the existing GitHub metadata adapter; no README/issues, account/CRM, or outreach. D3 adds an optional exact cutoff year to the current World Bank chronology check while preserving its “not a completed backtest” limits. D5 makes OnboardPath's public OPM text review user-selectable by a safely validated document number from the current admitted OPM metadata set and exposes bounded section-cited text, not an employee answer.
- **Context:** V1 receipt and relevant app README/source contract. No private CRM, company books, employee data, or external experiment record may be invented.
- **Verify:** worker's app tests and exact normal CLI; lead runs a fresh current-source workflow and inspects meaningful output/provenance/labels; paired blind text and scoring critic passes; each code piece includes thermo self-review and independent critic citations.
- **Preserve:** no fixture/cache substitution, no unverified terms or authorization, no claims that public data proves missing private inputs, no AI verification without current independent witness evidence, no customer/results claims.
- **DoD:** exactly three distinct eligible slices, each tied to V1 selection evidence, real admitted source receipt, meaningful reviewed output, and honest full-job boundary—or an honest measured negative/blocked result without a workaround.
- **Owner:** three sequential `gauntlet_worker` pieces after exact scope and budget registration.

### D4 — Local portfolio claim alignment (conditional)

- **Outcome:** live Pages project claims and local résumé/portfolio source are matched to fresh suite evidence; exact local corrections only if required.
- **Scope:** read all relevant live routes and current source first. Before edits, fix exact writable path list to only the relevant local site/resume files under `agentic-resume`; preserve its intake `.gitignore` modification and untracked `resume/__pycache__/`. External run index and report are read-only to delegated workers. If no mismatch exists, no page source is changed.
- **Do:** use a copy-only design brief preserving the current layout if implementation is required; change only demonstrably wrong or incomplete statements and keep all WIP/UNVERIFIED boundaries.
- **Context:** fresh V1 receipts, exact live route observations, local resume sources, `agentic-resume/README.md`, and sealed bar.
- **Verify:** local build/PDF check if source indicates a PDF change; fetch/check every changed local route; confirm no changes to remote Pages and preserve pre-existing dirty state. Worker runs thermo review and paired blind critics; lead independently reruns build and routes.
- **Preserve:** no visual redesign, style changes, adding unverified claims, account changes, deployment, push, PR, or publication.
- **DoD:** comparison ledger names match/overclaim/underclaim for each relevant claim; only exact necessary local edits are tested or documented as no-change; remote stays unchanged.
- **Owner:** if changes are needed, design director then delegated `gauntlet_frontend_builder` (worker-derived). Otherwise lead records no-change from fresh evidence.

### V2 — Lead-owned final verification and report

- **Outcome:** fresh final tree checks and the exact Lane 2 report at `/home/cayleb/Work/session-artifacts/four-lanes-cycle-20260925/lane-reports/lane2-portfolio.md`.
- **Scope:** lead writes the external report only at that exact path; project evidence remains under the two repository roots. No external run index/manifests are edited.
- **Verify:** replay decisive app paths and route checks after edits; run required full tests; compare Git status to intake; run Gauntlet validation where supported; persist required final memory with exact read-back.
- **DoD:** report states all ten per-app outcomes, evidence/exit codes, changes, current state, one-command verification recipe, and no more than one operator item; no false success and no remote publication.

## Execution order and decision gate

1. V1 is complete. No source edits were made before it.
2. R1 is complete. The researcher returned UNVERIFIED on the current API-docs rendering; the blind text critic endorsed no use. D1 ChainWatch is bailed before implementation. Do not treat the one earlier response as permission.
3. Deepen exactly three weakest eligible slices: PipelineRelay, BacktestGuard, and OnboardPath. Their baseline limitations and reasons are in `evidence/baseline-assessment.md`; no other app is deepened.
4. Run the three code pieces sequentially with file claims and frozen scopes. Each code piece has a worker thermo review, paired blind critics, and fresh lead-run end-to-end verification.
5. Run the design-director copy brief before D4; make only exact claim corrections in the local résumé/site/PDF source and clearly distinguish public WIP source from undeployed workflows and local unpublished edits.
6. Lead owns final end-to-end checks, local route/build checks, commits if appropriate, and the requested Lane 2 report. No live publishing action occurs.
